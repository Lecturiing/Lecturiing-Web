import OnboardingWizard from '../../components/dashboard/onboarding/OnboardingWizard';

export const metadata = { title: 'Profile Setup — Lecturiing' };

export default function OnboardingPage() {
  return <OnboardingWizard />;
}
