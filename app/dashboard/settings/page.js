import SettingsPage from '../../components/dashboard/settings/SettingsPage';
export const metadata = { title: 'Settings — Lecturiing' };
export default function Settings() {
  return <SettingsPage />;
}
