import VerificationPage from '../../components/dashboard/verification/VerificationPage';

export const metadata = { title: 'Verification — Lecturiing' };

export default function Verification() {
  return <VerificationPage />;
}
